# Frontend

This is the frontend for the application. It’s built with SvelteKit (TypeScript) and communicates with the .NET API backend.

---

## Prerequisites

- **Node.js**

From the `/frontend` folder, install dependencies:

```bash
  npm install
```

## Run Locally

```bash
  npm run dev
```

## Environment Variables
Create a .env file in the root of the /frontend folder

Use example.env to guide you.

